Java application to test Maven.

mvn -v : check installed version of Maven

mvn compile : create Target/*.class

mvn package : compile and package the code up in JAR file named ?.jar
			? is defined in the pom.xml


pom.xml : file to configure dependencies and pagkage name (artifactId)

